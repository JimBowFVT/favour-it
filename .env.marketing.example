# Browser-safe configuration only. Never put service-role or signing secrets here.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
# Use true only for a separate marketing build. Normal application builds leave this false.
REACT_APP_MARKETING_SITE_ONLY=false
