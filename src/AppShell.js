import { useEffect, useState } from 'react';
import App from './App';
import AuthGate from './components/AuthGate';
import ActivityCenter from './components/ActivityCenter';
import FavouritLoader from './components/FavouritLoader';
import AdminPanel from './components/AdminPanel';
import AdminMessageReports from './components/AdminMessageReports';
import UsernameGate from './components/UsernameGate';
import UsernameManager from './components/UsernameManager';
import DirectMessaging from './components/DirectMessaging';
import DirectMessageBridge from './components/DirectMessageBridge';
import SettingsLauncher from './components/SettingsLauncher';
import PublicProfileHost from './components/PublicProfileHost';
import FriendRequestCenter, { FriendRequestBadge } from './components/FriendRequestCenter';
import { isSupabaseConfigured, supabase } from './lib/supabase';
import { getCurrentProfile } from './lib/profile';
import { getMyUsernameStatus } from './lib/usernames';
import { claimDailyReward, getMyWallet } from './lib/wallet';
const SESSION_TIMEOUT_MS=6000,PROFILE_TIMEOUT_MS=8000,USERNAME_STATUS_TIMEOUT_MS=5000,REWARD_TIMEOUT_MS=5000,WALLET_TIMEOUT_MS=5000,USERNAME_ONBOARDING_KEY='favourit_username_onboarding_pending';
function withTimeout(promise,ms,message='Request timed out. Please try again.'){let timer;const timeout=new Promise((_,reject)=>{timer=window.setTimeout(()=>reject(new Error(message)),ms);});return Promise.race([promise,timeout]).finally(()=>window.clearTimeout(timer));}
function isAdminPanelPath(){return window.location.pathname.replace(/\/+$/,'')==='/adminpanel';}function usernameCacheKey(userId){return userId?`favourit_username:${userId}`:'';}function getPendingOnboarding(){try{return JSON.parse(localStorage.getItem(USERNAME_ONBOARDING_KEY)||'null');}catch(_){return null;}}
export default function AppShell(){const[session,setSession]=useState(null);const[wallet,setWallet]=useState(null);const[profile,setProfile]=useState(null);const[usernameStatus,setUsernameStatus]=useState(null);const[loading,setLoading]=useState(isSupabaseConfigured);const[uiReady,setUiReady]=useState(false);const[rewardMessage,setRewardMessage]=useState('');const adminPath=isAdminPanelPath();
useEffect(()=>{if(!isSupabaseConfigured||!supabase)return undefined;let mounted=true;let loadVersion=0;const hydrateInBackground=async nextSession=>{const version=++loadVersion;if(!nextSession){if(mounted&&version===loadVersion){setSession(null);setWallet(null);setProfile(null);setUsernameStatus(null);setRewardMessage('');setUiReady(true);setLoading(false);}return;}if(mounted&&version===loadVersion){setSession(nextSession);setLoading(false);setUiReady(false);}const userId=nextSession.user.id;const userEmail=nextSession.user.email?.toLowerCase();try{const profileResult=await withTimeout(getCurrentProfile(),PROFILE_TIMEOUT_MS,'Account data is taking too long.');if(!mounted||version!==loadVersion)return;setProfile(profileResult?.profile||null);if(profileResult?.wallet)setWallet(profileResult.wallet);else{try{const currentWallet=await withTimeout(getMyWallet(),WALLET_TIMEOUT_MS,'Wallet loading timed out.');if(mounted&&version===loadVersion)setWallet(currentWallet||null);}catch(_){}}}catch(error){if(mounted&&version===loadVersion)setRewardMessage(error.message||'Some account data could not be loaded yet.');}if(mounted&&version===loadVersion)setUiReady(true);try{const status=await withTimeout(getMyUsernameStatus(),USERNAME_STATUS_TIMEOUT_MS,'Username setup is taking too long.');if(!mounted||version!==loadVersion)return;const normalizedStatus=status?.username?{...status,username_chosen:true}:status||null;setUsernameStatus(normalizedStatus);if(normalizedStatus?.username){localStorage.setItem(usernameCacheKey(userId),normalizedStatus.username);const pending=getPendingOnboarding();if(pending&&((pending.userId&&pending.userId===userId)||pending.email===userEmail))localStorage.removeItem(USERNAME_ONBOARDING_KEY);}}catch(_){const cached=localStorage.getItem(usernameCacheKey(userId));if(cached&&mounted&&version===loadVersion)setUsernameStatus({username:cached,username_chosen:true});}if(!adminPath){try{const reward=await withTimeout(claimDailyReward(),REWARD_TIMEOUT_MS,'Daily reward timed out.');if(mounted&&version===loadVersion&&reward?.claimed){setRewardMessage(reward.reward_fav>0?`Daily reward: +${reward.reward_fav} FAV`:'Daily reward recorded.');try{const refreshedWallet=await withTimeout(getMyWallet(),WALLET_TIMEOUT_MS);if(mounted&&version===loadVersion)setWallet(refreshedWallet||null);}catch(_){}}}catch(rewardError){if(mounted&&version===loadVersion&&!String(rewardError?.message||'').toLowerCase().includes('already claimed'))setRewardMessage('Daily reward is unavailable right now.');}}};const initializeSession=async()=>{try{const{data}=await withTimeout(supabase.auth.getSession(),SESSION_TIMEOUT_MS,'Session check timed out.');if(!mounted)return;hydrateInBackground(data.session);}catch(_){if(mounted){setUiReady(true);setLoading(false);}}};initializeSession();const{data:listener}=supabase.auth.onAuthStateChange((event,nextSession)=>{window.setTimeout(()=>{if(!mounted)return;if(event==='SIGNED_OUT')hydrateInBackground(null);else hydrateInBackground(nextSession);},0);});return()=>{mounted=false;loadVersion+=1;listener.subscription.unsubscribe();};},[adminPath]);
useEffect(()=>{if(!session?.user?.id||!supabase||adminPath)return undefined;let active=true;const mark=()=>{if(active)supabase.rpc('set_my_presence',{p_online:true}).catch(()=>{});};mark();const timer=window.setInterval(mark,20000);const onVisible=()=>{if(document.visibilityState==='visible')mark();};document.addEventListener('visibilitychange',onVisible);const onUnload=()=>{try{supabase.rpc('set_my_presence',{p_online:false});}catch(_){}};window.addEventListener('beforeunload',onUnload);return()=>{active=false;clearInterval(timer);document.removeEventListener('visibilitychange',onVisible);window.removeEventListener('beforeunload',onUnload);};},[session?.user?.id,adminPath]);
if(!isSupabaseConfigured)return <div className="app-loading"><div><div className="logo"><span>Favour</span><i>it</i></div><h2>Connect your Favourit backend</h2><p>Add REACT_APP_SUPABASE_URL and REACT_APP_SUPABASE_ANON_KEY to the environment before launching.</p></div></div>;
if(loading||!uiReady)return <FavouritLoader title="Connecting to Favourit" subtitle="Checking your secure account…"/>;
if(!session)return <AuthGate/>;
if(adminPath)return <><AdminPanel/><main style={{maxWidth:1200,margin:'0 auto',padding:'0 24px 48px'}}><AdminMessageReports/></main></>;
const pending=getPendingOnboarding();const onboardingPending=Boolean(pending&&((pending.userId&&pending.userId===session.user.id)||pending.email===session.user.email?.toLowerCase()));if(onboardingPending&&!usernameStatus?.username)return <UsernameGate displayName={usernameStatus?.display_name||session.user.user_metadata?.display_name||''} email={usernameStatus?.email||session.user.email||''} onComplete={profileData=>{localStorage.removeItem(USERNAME_ONBOARDING_KEY);localStorage.setItem(usernameCacheKey(session.user.id),profileData.username);setUsernameStatus({...usernameStatus,...profileData,username_chosen:true});}}/>;
return <><App initialWallet={wallet} session={session} rewardMessage={rewardMessage} usernameStatus={usernameStatus}/><UsernameManager status={usernameStatus} onChanged={status=>{localStorage.setItem(usernameCacheKey(session.user.id),status.username);setUsernameStatus(status);}}/><DirectMessaging session={session}/><DirectMessageBridge/><PublicProfileHost session={session}/><ActivityCenter/><FriendRequestCenter/><FriendRequestBadge/><SettingsLauncher session={session} profile={profile} onProfileChanged={next=>setProfile(next)}/></>;
}
