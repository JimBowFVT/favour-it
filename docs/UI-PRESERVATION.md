# Favour-it interface constraints

Owner correction, 7 September 2026:

- Keep the original global top navbar. A sidebar request is not permission to replace it.
- The planned sidebar is scoped only to Explore. Do not mount it on Home, Orders, Community, Profile, settings or the application shell.
- Do not invent new wallet screens, navigation destinations, forms, card layouts or global styles while fixing wallet/token logic.
- Use the owner's actual design notes and existing code as the visual source. Do not infer the missing earlier design from another project or the marketing preview.
- Financial/auth fixes and design changes are separate. When an API needs an additional input, document the dependency and integrate it into the approved design rather than silently redesigning the interface.
- Preserve uncommitted local work and unrelated project changes. No force-reset of main or the user's worktree.
